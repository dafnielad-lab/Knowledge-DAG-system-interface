fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
jinja2>=3.1
markdown>=3.5
python-multipart>=0.0.9
python-frontmatter>=1.1
pyyaml>=6.0

# Test-only (kept here so `pip install -r` on a fresh machine sets up tests too)
pytest>=8.0
httpx>=0.27
